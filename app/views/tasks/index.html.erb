<h1>Things to do</h1>
<!-- 
<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Category</th>
      <th>Due date</th>
      <th>Is completed</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @tasks.each do |task| %>
      <tr>
        <td><%= task.name %></td>
        <td><%= task.category %></td>
        <td><%= task.due_date %></td>
        <td><%= task.is_completed %></td>
        <td><%= link_to 'Show', task %></td>
        <td><%= link_to 'Edit', edit_task_path(task) %></td>
        <td><%= link_to 'Destroy', task, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table> -->

  <!-- <form>
    <% @tasks.each do |task| %>
    <div class="row">
      <div class="large-12 columns">
        <%= check_box_tag(:task, task.id) %>
        <%= label_tag(:task, task.name) %>
         <%= link_to 'Show', task %>  |  <%= link_to 'Edit', edit_task_path(task) %>
      </div>
    
    </div>
    <% end %>
  </form> --> 
  <div class="row">
    <div class="large-8 columns">
      <%= form_tag complete_tasks_path, method: :patch do %>
        <%= render partial: 'tasklist', locals: {tasks: @tasks} %>
        <%= submit_tag "Mark as Complete", class: "button small success radius" %>        
      <% end %>
    </div>
    <div class="large-4 columns">
      <%= link_to 'New Task', new_task_path, class: "button raidus max-width" %>
      <%= form_tag search_tasks_path, method: :get do %>
        <div class="row collapse">
          <div class="small-9 columns">          
            <%= text_field_tag :search, nil, placeholder: "Type in the keywords" %>
          </div>
          <div class="small-3 columns">          
            <%= submit_tag "Search", class: "button postfix" %>
          </div>
        </div>
      <% end %>
    </div>
  </div>
  

<br>


